import * as React from 'react';
import { connect } from 'react-redux';

export class ActionDispatcher {

    /*
    static async buildFlow(): Promise<any>{
     
        // 送信
        await fetch(build.url,{
            method: 'post',
            mode: 'cors',
            headers: {
                'content-type': 'application/json'
            },
            body: build.body,
        }).then(resp => {
            console.log(`response: ${resp}`);

            // 応答をstateに登録

            // 応答ダイアログをopen

        });
    }
    */
}
